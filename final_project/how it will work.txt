Im planning for the way it will work is it will be a simple 500x5 nural network

it will have 1 input layer, 5 hidden, 1 output. the input layer will be 500 nurons
the hiddne layers will ALL be 500 nurons, the output layer will be only 1
maybe i will do 250 input, 500 1st
and slowly decrease the number of nerons per hidden layer, but that will be determined after testing, whichever version is most effective will be chosen. 

the first 100 charecters will be resereved for input, and the rest will be for the context window of the network. it will generate 1 word at a time, and each time it wants to make another word, i wil have it do the entire network again. it will be doing text prediction. 
this means it also wont be able to reply to previous messages because of extreemly limited context window, but theoretically, it should work as a mehtod for network output. i wonder if the way other llms work is similar to this.
my next steps should be the creation of a nural network generator function
it will take the parameters i give it, and auto wire up a network, since that is the most time consuming thing for me. 
im not exarctly sure what the purpose of the 1000x1000 matrix is for, maybe ill do more complex math with it lateor, or once its filled out i will run the words through it before the input, still unsure for now.

